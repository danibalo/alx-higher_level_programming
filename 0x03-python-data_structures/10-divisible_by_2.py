#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    new_lst = []
    for i in my_list:
        if i % 2 == 0:
            new_lst.append(True)
        else:
            return new_lst.append(False)
    return(new_lst)
